<!--
Title: `type(scope): what the system now does`, as a sentence.
  e.g. feat(chat): the composer keeps its draft across a provider switch
Scopes: core, ui, app, docs, ci, scripts, tests, deps
-->

## What the system now does

<!-- One or two sentences. The reader should be able to try it. -->

## How it is kept true

<!-- Which test, script or reading backs the claim above. -->

## Checklist

- [ ] `make ci` passes locally
- [ ] Any README sentence this changes is still true, and its `<!-- test: -->` marker still names a real test
- [ ] A decision worth revisiting has an ADR in `docs/adr/` with a kill criterion that names a reading
- [ ] No credential can reach a log line
